# Frontend config. Copy to ".env".
#
# Point this at your running Novora backend to go LIVE against PostgreSQL.
# Leave it blank/unset to run in local demo mode (browser storage only).
VITE_API_URL=http://localhost:4000
